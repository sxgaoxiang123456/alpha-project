# 会话交接 · 基础实时行情

## 上次做到哪
T06 已完成：QuoteService 已集成 F2 CacheService，清洗后的自选股行情会按 `quote:{code}` 写入缓存，TTL 为 5 分钟，并通过单元测试验证命中与过期失效。

## 下次会话要做的事
1. 先读宪法（constitution.md）
2. 读 state.md → 当前任务是 T07
3. 从 T07 继续，禁止重新规划

## 禁止重新规划
plan.md 已经定稿，tasks.md 已经锁定。
直接执行，不要再 re-plan。
