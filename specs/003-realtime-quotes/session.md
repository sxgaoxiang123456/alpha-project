# 会话交接 · 基础实时行情

## 上次做到哪
T05 已完成：QuoteService 已创建，可从 WatchlistItem 读取自选股代码，批量调用 facade 获取实时行情，并通过 DataCleaner 返回标准化 Quote 列表。

## 下次会话要做的事
1. 先读宪法（constitution.md）
2. 读 state.md → 当前任务是 T06
3. 从 T06 继续，禁止重新规划

## 禁止重新规划
plan.md 已经定稿，tasks.md 已经锁定。
直接执行，不要再 re-plan。
